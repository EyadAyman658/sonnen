import { moderateScale } from 'react-native-size-matters';

export const typography = {
  fontSizes: {
    sm: moderateScale(12),
    md: moderateScale(16),
    lg: moderateScale(20),
    xl: moderateScale(24),
  },
  fontWeights: {
    regular: '400',
    bold: '700',
  },
};
